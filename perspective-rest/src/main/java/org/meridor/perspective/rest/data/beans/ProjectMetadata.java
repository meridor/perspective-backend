package org.meridor.perspective.rest.data.beans;

public class ProjectMetadata extends EntityMetadata {
    public ProjectMetadata(String entityId, String key, String value) {
        super(entityId, key, value);
    }
}
