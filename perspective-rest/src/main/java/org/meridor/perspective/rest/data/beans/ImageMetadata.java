package org.meridor.perspective.rest.data.beans;

public class ImageMetadata extends EntityMetadata {
    
    public ImageMetadata(String entityId, String key, String value) {
        super(entityId, key, value);
    }
    
}
